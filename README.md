# mirror-xyz
Simple script for creating enantiomeric xyz files

It mirrors molecular coordinates at the yz-plane by default. Change the "plane" in "mirror_xyz" function to mirror across other coordinate planes.

Keep your input .xyz file and script in the same folder and run:
"python mirror_xyz.py input.xyz output.xyz"
